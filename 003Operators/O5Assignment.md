# Assignment

- C lets us shorthand performing math on a variable's starting value with: +=, -=, *=, /= and %=

Instead of:
```c
int a = 2;
int b = 3;
a = a + b;
````

you can write:
```c
int a = 2;
int b = 3;
a += b;
```