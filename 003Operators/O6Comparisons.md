# Comparisons

- check if two values are:

| symbol | meaning |
|---|---|
| == | equal |
| != | not equal |
| > | greater than |
| >= | greater than or equal |
| < | less than |
| <= | less than or equal |

Example:
```c
int a = 3;
int b = 3;
if (a == b) {
  a++;
}```